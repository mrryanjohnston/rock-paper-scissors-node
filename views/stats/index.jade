h1= title
p These are the stats, currently. 
  - if (user || sess.displayName)
    a(href='../stats/'+user.displayName||sess.displayName) Your stats 
table#stats
  tr
    th Name
    th Wins
    th Losses
  !=partial('founduser', foundusers);
  
